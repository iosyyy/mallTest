spring:
  profiles:
    active: db, core, admin, wx
  messages:
    encoding: UTF-8
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

server:
  port: 8080

logging:
  config: classpath:logback-spring.xml

swagger:
  production: false